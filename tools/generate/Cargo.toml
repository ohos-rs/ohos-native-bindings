[package]
name    = "generate"
version = "0.1.0"
edition = "2021"
publish = false
build   = "build/main.rs"

[build-dependencies]
bindgen   = { workspace = true }
once_cell = { workspace = true }
anyhow    = { workspace = true }
regex     = { workspace = true }
