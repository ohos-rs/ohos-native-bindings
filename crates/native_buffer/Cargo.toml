[package]
name        = "ohos-native-buffer-binding"
version     = "0.0.1"
edition     = "2021"
license     = "MIT OR Apache-2.0"
description = "OpenHarmony's native buffer binding for rust"

[dependencies]
ohos-native-buffer-sys = { workspace = true }
ohos-native-window-sys = { workspace = true }
