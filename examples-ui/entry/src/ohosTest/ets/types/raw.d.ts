/* auto-generated by OHOS-RS */
/* eslint-disable */

import resourceManager from "@ohos.resourceManager";
/** Whether the given rawfile path is a directory. */
export declare function isRawDir(
  resourceManager: resourceManager.ResourceManager,
  path: string,
): boolean;

export declare function openRawDir(
  resourceManager: resourceManager.ResourceManager,
  dir: string,
): void;

/**
 * Open a rawfile, read it fully and return its byte length plus a short
 * text preview.
 */
export declare function readRawFile(
  resourceManager: resourceManager.ResourceManager,
  fileName: string,
): string;

/** Seek around in a rawfile to demonstrate offset/remain APIs. */
export declare function seekRawFile(
  resourceManager: resourceManager.ResourceManager,
  fileName: string,
): string;
